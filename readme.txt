Run the command:
npm start     --> This is going to run the script "start" set up in the package.json file

Example to see the use of the express library and handlebars

Open the browser and type:
http://localhost:3000
